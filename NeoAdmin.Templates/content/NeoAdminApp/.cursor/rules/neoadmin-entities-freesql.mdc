---
description: NeoAdmin 实体定义与 FreeSql 数据访问约定
globs: "**/Entities/**/*.cs"
alwaysApply: false
---

# 实体与 FreeSql

## 基类继承链

```
Entity → EntityCreated → EntityModified → EntityAudited
```

| 基类 | 场景 |
|------|------|
| `Entity` | 简单关联表、日志 |
| `EntityCreated` | 只需创建信息 |
| `EntityModified` | 常规业务表（默认首选） |
| `EntityAudited` | 需审批流（配合 `IsWorkflowAudit`） |

## 实体模板

```csharp
using FreeSql.DataAnnotations;
using NeoAdmin.Blazor.Entities;

namespace NeoAdminApp.Entities.Blog;

[Table(Name = "blog_classify")]
public class Classify : EntityModified
{
    [Column(StringLength = 50)]
    public string ClassifyName { get; set; } = string.Empty;
    public int SortCode { get; set; }
}
```

## FreeSql 常用写法

```csharp
// 注入：@inject IFreeSql FreeSql 或构造函数注入

var list = await FreeSql.Select<Classify>()
    .Where(a => a.ClassifyName.Contains(keyword))
    .OrderByDescending(a => a.Id)
    .ToListAsync();

await FreeSql.Update<Classify>()
    .Set(a => a.ArticleCount, count)
    .Where(a => a.Id == id)
    .ExecuteAffrowsAsync();

var articles = await FreeSql.Select<Article>()
    .Include(a => a.Classify)
    .ToListAsync();
```

## 表结构同步（宿主 SeedData）

```csharp
public static void SyncStructure(IFreeSql freeSql)
{
    freeSql.CodeFirst.SyncStructure<MyItem>();
    freeSql.CodeFirst.SyncStructure<MyItem.MyRelation>(); // 多对多中间表也需单独 Sync
}
```

## 框架特性

| 特性 | 用途 |
|------|------|
| `[Snowflake]` | 雪花主键 |
| `[Scheduler("id", "cron")]` | 定时任务 |
| `[Transactional]` | 方法级事务 |
| `[AntiConcurrency]` | 防并发 |

`CrudTable` 未指定 `LoadFunc`/`OnSaveAsync`/`OnDeleteAsync` 时，默认用 `IFreeSql` 自动 CRUD。
